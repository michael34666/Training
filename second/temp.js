/* import{ useState } from 'react';

//
function Sentence({text,onclick}) {
  return (
    <button onClick={onclick}>
      {text}
    </button>
  );
}


/*if(addOrNot){
   const newList=list.concat({});
   setlist(newList);
  }

  return null;
  */
/*
function TodolistSwitch({x, uncompTasks}) {
  switch(x)
  {
    case add:
      return null;
      uncompTasks++;
    case remove:
      return null;
      uncompTasks--;
    case completed:
      return null;
      uncompTasks--;
    default:
      return null;
    
  }
  
}*/

/*
export default function MyApp() {
  const [addOrNot, setAddOrNot] = useState(true);
  const [uncompTasks, setUncompTasks] = useState(0);

  const intialList = []
  const[list,setlist] = useState(intialList);
  return (
    <div>
      <Todolist addOrNot={addOrNot} />
    </div>

  );
}
*/